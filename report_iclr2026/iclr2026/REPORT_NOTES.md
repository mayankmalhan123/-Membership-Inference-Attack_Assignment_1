# Report Notes

Before exporting the final PDF, update these two fields in `iclr2026_conference.tex`:

1. Confirm the author line is correct.
2. Replace `TO_BE_FILLED_AFTER_FIRST_SUBMISSION` with your best public leaderboard score.

The report already includes:

- the required GitHub repository URL
- no abstract, matching the course instructions
- the official ICLR 2026 style files

To build it on Overleaf:

1. Open the official ICLR template project or upload this `iclr2026` folder.
2. Set `iclr2026_conference.tex` as the main file.
3. Compile with pdfLaTeX.
